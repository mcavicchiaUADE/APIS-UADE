{
  "name": "ecommerce-backend-legacy",
  "version": "1.0.0",
  "description": "Backend legacy usando JSON Server - NO USAR",
  "private": true,
  "scripts": {
    "server": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {},
  "keywords": ["json-server", "legacy", "deprecated"],
  "author": "E-commerce Team",
  "license": "MIT",
  "note": "Este backend ya no se usa. El proyecto actual usa Spring Boot."
}
